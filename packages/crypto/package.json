{
  "name": "@afanta/crypto",
  "version": "0.1.0",
  "private": true,
  "description": "AFANTA — Envelope Encryption (Argon2id + AES-256-GCM + AES-KW)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "argon2": "^0.44.0"
  },
  "devDependencies": {}
}
